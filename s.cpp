//divide the array into 2 equal half such that absolute diff is minimum.
#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
	float f[5] = {1.0,1.2,1.3,1.4,1.5};
	float* f1 = &f[0];
	float* f2 = f1 + 3;
	cout<<f2 - f1;
	/*vector<int> v1;
	int n,item;
	cin>>n;
	while(n--){
		cin>>item;
		v1.push_back(item);
	}
	cout<<findEqualHalf(v1)
	*/return 0;
}