#include <bits/stdc++.h>
using namespace std;
int main(){
	/*char str[1000];
	string ans,sstr;
	cin>>str;
	int no,exNo;
	int n = strlen(str);
	for(int i=0;i<n;i++){
		exNo = str[i];
		while(!(str[i]>='0') && !(str[i]<='9')){
			sstr += str[i];
			i++;
		}
		cout<<sstr;
	}
	*/
	cout<<"hi";
	/*char *str;
	cin>>str;
	cout<<strlen(str);
	*/return 0;
}