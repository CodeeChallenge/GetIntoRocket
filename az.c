#include <stdio.h>   
int main()
{
	union A{
	if (a)
	  free(i);
};
int *A,*stack;
int B[10]={0};
A=(int*)malloc[10];
o<calloc(B+1)
for(int o=0;o<10;o++)
	struct ~(A+i);
if (stack==full && A!=0)
	printf("the stack is full");
else 
	printf("space left");
return 0;
}